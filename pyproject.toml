[tool.poetry]
name = "flaskweb"
version = "0.1.0"
description = ""
authors = ["Replit user <<>>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
flask = "3.0.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
